conda create -n "YOURENV" python=3.11 ipython
pip install fastapi==0.111.1

conda config --append channels conda-forge
conda install -c pytorch faiss-gpu -n "YOURENV"

pip install validators
pip install requests
pip install rank_bm25
pip install tqdm
!pip install retriv

conda install pytorch-cuda=12.1 -c pytorch -c nvidia -y
conda install pytorch torchvision torchaudio -c pytorch -c nvidia -y

conda clean --all

data https://www.blackhatworld.com/seo/get-10-000-images-from-instagram-mediafire-links.600292/